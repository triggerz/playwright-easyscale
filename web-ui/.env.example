# Orchestrator API URL (backend) - must start with VITE_ to be exposed to browser
VITE_ORCHESTRATOR_URL=http://localhost:3000

# Public URL of this web-ui app (for Vite host validation)
VITE_PUBLIC_URL=http://localhost:5173
